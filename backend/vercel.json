{
    "version": 2,
    "builds": [
        {
            "src": "/api/server.js",
            "use": "@vercel/node"
        } 
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/api/server.js"
        }
    ]
}